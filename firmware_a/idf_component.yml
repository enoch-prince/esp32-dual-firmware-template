## IDF Component Manager Manifest File
dependencies:
  ## Required IDF version
  idf:
    version: '>=4.1.0'
  # # Put list of dependencies here
  # # For components maintained by Espressif:
  # component: "~1.0.0"
  # # For 3rd party components:
  # username/component: ">=1.0.0,<2.0.0"
  # username2/component2:
  #   version: "~1.0.0"
  #   # For transient dependencies `public` flag can be set.
  #   # `public` flag doesn't have an effect dependencies of the `main` component.
  #   # All dependencies of `main` are public by default.
  #   public: true
  # espressif/esp-idf-cxx: ^1.0.2-beta
  # espressif/cjson: ^1.7.19~2
  espressif/esp_mqtt: "^2.0"
  espressif/esp_http_client: "^1.0"
  espressif/esp_http_server: "^1.0"
  # espressif/nvs_flash: "^2.0"
  # espressif/esp_mbedtls: "^1.0"
  # espressif/esp_crypto: "^1.0"   # ✅ Optional: ESP-IDF crypto wrappers

  # ✅ Add dependency on your local expected component
  expected:
    path: ../components/expected
  
  etl:
    path: ../components/etl
  
  jsmn:
    path: ../components/jsmn
